version: '3.8'
services:
  web:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=supersecret
      - AWS_REGION=us-east-1
      - SNS_TOPIC_ARN=
    volumes:
      - ./backend:/app

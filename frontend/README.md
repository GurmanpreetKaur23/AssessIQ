# Frontend

This is a minimal React (Vite) frontend for AssessIQ.

Environment
- VITE_BACKEND_BASE_URL (default: http://localhost:8000)

Run locally
- cd frontend
- npm install
- npm run dev

Notes
- The frontend expects the backend endpoints added in the feature/frontend-integration branch to be available.
